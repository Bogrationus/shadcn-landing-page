import type { ReactNode } from "react";

export const metadata = {
  title: "My New Landing",
  description: "Modern landing built with shadcn/ui",
};

export const Layout = ({ children }: { children: ReactNode }) => children;
